<% include ../partials/header %>

      <div class="starter-template">
        <h1>Notícias admin</h1>
        <table>
          <% news.forEach(obj => { %>
          <tr>
            <td><%= obj.title %></td>
            <td><%= obj.category %></td>
          </tr>
          <% }) %>
        </table>
      </div>

<% include ../partials/footer %>